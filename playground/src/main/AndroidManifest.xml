<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".player.App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Exursion"
        android:networkSecurityConfig="@xml/network_security_config">


        <service android:name=".player.MediaPlayerService"
            android:exported="true">
            <intent-filter>
                <action android:name="ru.exursion.action.PLAY"/>
                <action android:name="ru.exursion.action.PAUSE"/>
                <action android:name="ru.exursion.action.RESUME"/>
                <action android:name="ru.exursion.action.STOP"/>
                <data android:mimeType="text/plain"/>
            </intent-filter>
        </service>

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>